events {
}

http {

    upstream backend_up {
        server backend:5000;   
    }

    server {
        listen 80;
        server_name day4.new;

        return 301 https://$host$request_uri;
        
    }

    server {
        listen 443 ssl;
        server_name day4.new;

        ssl_certificate      /etc/nginx/certs/day4.new.pem;
        ssl_certificate_key  /etc/nginx/certs/day4.new-key.pem;

        location / {
            proxy_pass http://backend_up;
        }
    }
}
